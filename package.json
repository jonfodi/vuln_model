{
  "name": "vulnerability-model",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio",
    "ingest": "bun src/ingest/cli.ts",
    "ingest:seed": "bun src/ingest/cli.ts seed",
    "ingest:cve": "bun src/ingest/cli.ts cve",
    "api:start": "bun src/api/server.ts",
    "api:dev": "bun --hot src/api/server.ts"
  },
  "dependencies": {
    "drizzle-orm": "latest",
    "postgres": "latest"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "latest",
    "drizzle-kit": "latest",
    "typescript": "latest"
  }
}
